export function formatToPixels(value) {
  return `${value}px`;
}
export function getHorizontalCenter(position) {
  return position.left + position.width/2;
}

export function getVerticalCenter(position) {
  return position.top + position.height/2;
}